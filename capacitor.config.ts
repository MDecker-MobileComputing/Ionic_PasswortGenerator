import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.mide.ionic.passwortgenerator',
  appName: 'Passwort-Generator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
